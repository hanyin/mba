package com.hy.mvp.greendao.gen;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import com.hy.mvp.greendao.bean.StringConverter;
import java.util.List;

import com.hy.mvp.greendao.bean.Drafts;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "DRAFTS".
*/
public class DraftsDao extends AbstractDao<Drafts, Long> {

    public static final String TABLENAME = "DRAFTS";

    /**
     * Properties of entity Drafts.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property Typename = new Property(1, String.class, "typename", false, "TYPENAME");
        public final static Property Date = new Property(2, String.class, "date", false, "DATE");
        public final static Property Imagepaths = new Property(3, String.class, "imagepaths", false, "IMAGEPATHS");
    }

    private final StringConverter imagepathsConverter = new StringConverter();

    public DraftsDao(DaoConfig config) {
        super(config);
    }
    
    public DraftsDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"DRAFTS\" (" + //
                "\"_id\" INTEGER PRIMARY KEY AUTOINCREMENT ," + // 0: id
                "\"TYPENAME\" TEXT," + // 1: typename
                "\"DATE\" TEXT," + // 2: date
                "\"IMAGEPATHS\" TEXT);"); // 3: imagepaths
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"DRAFTS\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, Drafts entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        String typename = entity.getTypename();
        if (typename != null) {
            stmt.bindString(2, typename);
        }
 
        String date = entity.getDate();
        if (date != null) {
            stmt.bindString(3, date);
        }
 
        List imagepaths = entity.getImagepaths();
        if (imagepaths != null) {
            stmt.bindString(4, imagepathsConverter.convertToDatabaseValue(imagepaths));
        }
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, Drafts entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        String typename = entity.getTypename();
        if (typename != null) {
            stmt.bindString(2, typename);
        }
 
        String date = entity.getDate();
        if (date != null) {
            stmt.bindString(3, date);
        }
 
        List imagepaths = entity.getImagepaths();
        if (imagepaths != null) {
            stmt.bindString(4, imagepathsConverter.convertToDatabaseValue(imagepaths));
        }
    }

    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    @Override
    public Drafts readEntity(Cursor cursor, int offset) {
        Drafts entity = new Drafts( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1), // typename
            cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2), // date
            cursor.isNull(offset + 3) ? null : imagepathsConverter.convertToEntityProperty(cursor.getString(offset + 3)) // imagepaths
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, Drafts entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setTypename(cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1));
        entity.setDate(cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2));
        entity.setImagepaths(cursor.isNull(offset + 3) ? null : imagepathsConverter.convertToEntityProperty(cursor.getString(offset + 3)));
     }
    
    @Override
    protected final Long updateKeyAfterInsert(Drafts entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    @Override
    public Long getKey(Drafts entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    @Override
    public boolean hasKey(Drafts entity) {
        return entity.getId() != null;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
