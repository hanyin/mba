package com.hy.mvp.greendao.gen;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import com.hy.mvp.greendao.bean.AppConfig;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "APP_CONFIG".
*/
public class AppConfigDao extends AbstractDao<AppConfig, Long> {

    public static final String TABLENAME = "APP_CONFIG";

    /**
     * Properties of entity AppConfig.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property Config_name = new Property(1, String.class, "config_name", false, "CONFIG_NAME");
        public final static Property Config_value = new Property(2, String.class, "config_value", false, "CONFIG_VALUE");
    }


    public AppConfigDao(DaoConfig config) {
        super(config);
    }
    
    public AppConfigDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"APP_CONFIG\" (" + //
                "\"_id\" INTEGER PRIMARY KEY ," + // 0: id
                "\"CONFIG_NAME\" TEXT," + // 1: config_name
                "\"CONFIG_VALUE\" TEXT);"); // 2: config_value
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"APP_CONFIG\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, AppConfig entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        String config_name = entity.getConfig_name();
        if (config_name != null) {
            stmt.bindString(2, config_name);
        }
 
        String config_value = entity.getConfig_value();
        if (config_value != null) {
            stmt.bindString(3, config_value);
        }
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, AppConfig entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        String config_name = entity.getConfig_name();
        if (config_name != null) {
            stmt.bindString(2, config_name);
        }
 
        String config_value = entity.getConfig_value();
        if (config_value != null) {
            stmt.bindString(3, config_value);
        }
    }

    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    @Override
    public AppConfig readEntity(Cursor cursor, int offset) {
        AppConfig entity = new AppConfig( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1), // config_name
            cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2) // config_value
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, AppConfig entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setConfig_name(cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1));
        entity.setConfig_value(cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2));
     }
    
    @Override
    protected final Long updateKeyAfterInsert(AppConfig entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    @Override
    public Long getKey(AppConfig entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    @Override
    public boolean hasKey(AppConfig entity) {
        return entity.getId() != null;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
